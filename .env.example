# Copy to .env (gitignored) for local development. App Service reads the same names from its settings.
AZURE_OPENAI_ENDPOINT=https://<resource>.services.ai.azure.com/openai/v1/
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
AZURE_OPENAI_API_KEY=
# Optional: minimal | low | medium (default low)
AZURE_OPENAI_REASONING=low
# Optional locally; without it the server uses a local PostgreSQL named frostedcorner
DATABASE_URL=sqlite:///tmp/frostedcorner-dev.db
